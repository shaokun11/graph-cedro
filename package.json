{
  "name": "cedro",
  "version": "0.1.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build --network mumbai",
    "create": "graph create cedro/server --node http://localhost:8020",
    "deploy:l": "graph deploy cedro/server --node http://localhost:8020 --ipfs http://localhost:5001",
    "deploy": "graph deploy --product hosted-service shaokun11/cedrov2"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.30.2",
    "@graphprotocol/graph-ts": "^0.27.0"
  },
  "dependencies": {
    "babel-polyfill": "^6.26.0",
    "babel-register": "^6.26.0"
  }
}
